<?xml version="1.0" encoding="utf-8"?>
<!-- Network Security Configuration for Android 9+ -->
<network-security-config>
    <!--
    This app is 100% OFFLINE - No network requests
    Cleartext traffic is disabled for security
    -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Only trust system certificates -->
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!--
    If you ever need to add HTTPS endpoints in the future:
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">yourdomain.com</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>
    -->
</network-security-config>
